export default class Lembrete {
    constructor({id, descricao, data, concluida_em}) {
        this.id = id
        this.descricao = descricao
        this.data = data
        this.concluida_em = concluida_em
    }
}
