import React from 'react';
import {View, Text} from 'react-native';

export default function CriarMeta() {

    return (
        <View style={{alignItems: 'center', justifyContent: 'center', flex: 1}}>
            <Text>Criar Meta</Text>
        </View>
    );
}